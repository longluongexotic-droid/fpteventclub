// Public client configuration only. Never put service_role keys or passwords here.
// The portal remains unavailable until the member database is connected.
window.FEV_MEMBER_CONFIG = Object.freeze({
  supabaseUrl: '',
  publishableKey: ''
});
